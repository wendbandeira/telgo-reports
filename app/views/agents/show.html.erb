<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @agent.name %>
</p>

<p>
  <strong>Codeagent:</strong>
  <%= @agent.codeagent %>
</p>

<%= link_to 'Editar', edit_agent_path(@agent), class: 'button [secondary large]' %> 
<%= link_to 'Voltar', agents_path, class: 'button [secondary large]' %>
